---
interface Props {
  size?: "default" | "wide";
}

const { size = "default" } = Astro.props;
---

<div class:list={["academy-container", size === "wide" && "academy-container--wide"]}>
  <slot />
</div>
