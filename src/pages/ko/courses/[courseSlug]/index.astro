---
import CoursePage from "../../../../views/CoursePage.astro";
import { getCourseStaticPaths } from "../../../../views/route-helpers";

export async function getStaticPaths() {
  return getCourseStaticPaths();
}

const { courseSlug } = Astro.params;
---

<CoursePage locale="ko" courseSlug={courseSlug ?? ""} />
