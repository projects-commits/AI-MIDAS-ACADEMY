# Component patterns

## Matching content to component

- Use `BlackboardPanel` for compressed reasoning
- Use `FormulaBlock` for compact quantitative logic
- Use comparison cards for two-sided distinctions
- Use a decision tree for branching choices
- Use a failure mode card when the mistake must stand alone
- Use local labs when the learner benefits from changing inputs
